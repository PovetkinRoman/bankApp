apiVersion: v1
kind: Secret
metadata:
  name: {{ include "exchange.fullname" . }}-secret
  labels:
    {{- include "exchange.labels" . | nindent 4 }}
type: Opaque
stringData:
  oauth2-client-secret: {{ .Values.oauth2ClientSecret | default "exchange-secret-key-12345" | quote }}

